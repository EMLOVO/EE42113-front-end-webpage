name: Deploy to Cloudflare Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Set PROJECT_DIR to the path containing package.json (default is repo root '.')
    env:
      PROJECT_DIR: '.'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug repository layout (list files & locate package.json)
        run: |
          echo "Repository root files:"
          ls -la
          echo "Searching for package.json files (maxdepth 5):"
          find . -maxdepth 5 -name package.json -print || true
        shell: bash

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        # Use npm ci when a lockfile exists, otherwise fall back to npm install
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          if [ -f package-lock.json ] ; then
            echo "Found package-lock.json, running npm ci"
            npm ci
          else
            echo "No package-lock.json found, running npm install to generate a lockfile"
            npm install
          fi
        shell: bash

      - name: Build
        working-directory: ${{ env.PROJECT_DIR }}
        run: npm run build

      - name: Publish to Cloudflare Pages (Wrangler)
        # Uses Wrangler to publish the `build` directory to Pages
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          echo "Publishing from project dir: ${{ env.PROJECT_DIR }}"
          npx wrangler@3 pages publish build --project-name="${{ secrets.CF_PROJECT_NAME }}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        shell: bash
